<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('panel_fields', function (Blueprint $table) {
            $table->id();
            $table->foreignId('panel_id')->constrained()->onDelete('cascade');
            $table->string('name');
            $table->string('label');
            $table->string('type'); // Laravel migration type: string, text, integer, etc.
            $table->string('filament_type'); // Filament form field type: TextInput, Select, etc.
            $table->string('column_type'); // Filament table column type: TextColumn, etc.
            $table->boolean('nullable')->default(false);
            $table->string('default')->nullable();
            $table->json('validation_rules')->nullable();
            $table->integer('order')->default(0);
            $table->json('field_config')->nullable(); // Additional field-specific configurations
            $table->timestamps();

            $table->index(['panel_id', 'order']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('panel_fields');
    }
};
